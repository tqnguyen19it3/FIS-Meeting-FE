import React from 'react'

const Button = ({label, type, onClick}) => {

const buttonColor = type == 'cancel' ? "red" : "blue"
  return (
    // <button onClick={} style={{backgroundColor: buttonColor}} className=''>{label}</button>
  )
}

export default Button